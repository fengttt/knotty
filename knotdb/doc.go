// Package knotdb loads and indexes the knot dataset (tables + images)
// from the repo's dataset directory, and exposes lookup by name or
// notation along with access to diagrams and pre-computed invariants.
package knotdb
